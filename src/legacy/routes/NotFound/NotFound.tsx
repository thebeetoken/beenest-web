import * as React from 'react';

import NotFoundContainer from './NotFound.container';

const NotFound = () => (
  <NotFoundContainer>
    <h1>404</h1>
    <p>The page you requested is unavailable or does not exist. Please try again at a later time or navigate back.</p>
  </NotFoundContainer>
);

export default NotFound;
