import styled from 'styled-components';

const AdminUsersContainer = styled.section`
  .user-searchbar {
    display: flex;
    position: absolute;
    right: 0;
    top: -104px;
    button {
      margin-left: 24px;
    }
  }
`;

export default AdminUsersContainer;
